import stream from './stream'
import runtime from '../runtime'

let hooks = []

function httpPOST (uri, data) {

}

stream.on('forum:topic', (data) => {

})

stream.on('forum:post', (data) => {

})

stream.on('blog:post', (data) => {

})

stream.on('blog:comment', (data) => {

})

stream.on('user:new', (data) => {

})

function initiate () {
  // body...
}

module.exports = initiate()
